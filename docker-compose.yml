version: '3.8'

services:
  pusula-serve:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ADDR=:8080
    restart: always

  prometheus:
    image: prom/prometheus:latest
    ports:
      - "9090:9090"
    volumes:
      - ./ops:/etc/prometheus